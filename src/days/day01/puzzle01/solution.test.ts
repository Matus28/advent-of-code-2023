import { main } from "./solution";

describe("solution module for day01-puzzle01", () => {
  test("test if the solution - Calibration value is 54968", () => {
    expect(main()).toBe(54968);
  });
});
